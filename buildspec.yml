version: 0.2

env:
  exported-variables:
    - FLAG

phases:
  build:
    commands:
      - export FLAG=$(aws secretsmanager get-secret-value --secret-id flag_codebuild_lab_5 --query SecretString --output text 2>/dev/null || echo "no_access")
      - echo "FLAG=$FLAG"
