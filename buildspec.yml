version: 0.2

phases:
  build:
    commands:
      - echo "Running CodeBuild Lab 5 scoped flag retrieval"
      - aws secretsmanager get-secret-value --secret-id flag_codebuild_lab_5 --query SecretString --output text
      - echo "Done"
