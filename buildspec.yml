version: 0.2

phases:
  build:
    commands:
      - exec > >(tee build.log) 2>&1
      - echo "Running vulnerable training build"
      - env | sort | head -n 20
      - echo "Done"
      
artifacts:
  files:
    - build.log
